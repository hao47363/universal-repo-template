name: CI

on:
  push:
    branches:
      - "**"
    paths-ignore:
      - "**/*.md"
      - "governance-pack/docs/**"
      - ".github/*.md"
      - ".github/ISSUE_TEMPLATE/**"
      - ".github/pull_request_template.md"
  pull_request:
    branches:
      - "**"
    paths-ignore:
      - "**/*.md"
      - "governance-pack/docs/**"
      - ".github/*.md"
      - ".github/ISSUE_TEMPLATE/**"
      - ".github/pull_request_template.md"

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  prepare:
    name: Prepare config and change filters
    runs-on: ubuntu-latest
    outputs:
      run_lint: ${{ steps.cfg.outputs.run_lint }}
      run_test: ${{ steps.cfg.outputs.run_test }}
      run_build: ${{ steps.cfg.outputs.run_build }}
      cache_enabled: ${{ steps.cfg.outputs.cache_enabled }}
      cache_path: ${{ steps.cfg.outputs.cache_path }}
      cache_key: ${{ steps.cfg.outputs.cache_key }}
      cache_restore_keys: ${{ steps.cfg.outputs.cache_restore_keys }}
      run_heavy_checks: ${{ steps.filter.outputs.run_heavy_checks }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Read project config
        id: cfg
        shell: bash
        run: |
          set -euo pipefail
          run_lint="$(sh ./governance-pack/scripts/run_project_checks.sh enabled lint)"
          run_test="$(sh ./governance-pack/scripts/run_project_checks.sh enabled test)"
          run_build="$(sh ./governance-pack/scripts/run_project_checks.sh enabled build)"
          cache_enabled="$(sh ./governance-pack/scripts/get_config_value.sh cache.enabled false || true)"
          cache_path="$(sh ./governance-pack/scripts/get_config_value.sh cache.path "" || true)"
          cache_key="$(sh ./governance-pack/scripts/get_config_value.sh cache.key "" || true)"
          cache_restore_keys="$(sh ./governance-pack/scripts/get_config_value.sh cache.restore_keys "" || true)"

          echo "run_lint=${run_lint:-true}" >> "$GITHUB_OUTPUT"
          echo "run_test=${run_test:-true}" >> "$GITHUB_OUTPUT"
          echo "run_build=${run_build:-false}" >> "$GITHUB_OUTPUT"
          echo "cache_enabled=${cache_enabled:-false}" >> "$GITHUB_OUTPUT"
          echo "cache_path=${cache_path:-}" >> "$GITHUB_OUTPUT"
          echo "cache_key=${cache_key:-}" >> "$GITHUB_OUTPUT"
          echo "cache_restore_keys=${cache_restore_keys:-}" >> "$GITHUB_OUTPUT"

      - name: Detect code changes
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            run_heavy_checks:
              - '**'
              - '!**/*.md'
              - '!governance-pack/docs/**'
              - '!.github/*.md'
              - '!.github/ISSUE_TEMPLATE/**'
              - '!.github/pull_request_template.md'

  validate-governance:
    name: Validate naming and commit/PR conventions
    runs-on: ubuntu-latest
    needs: prepare
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate branch name
        env:
          BRANCH_NAME: ${{ github.event_name == 'pull_request' && github.head_ref || github.ref_name }}
        run: sh ./governance-pack/scripts/validate_branch_name.sh "$BRANCH_NAME"

      - name: Validate commit messages
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${{ github.event_name }}" == "push" ]]; then
            EXEMPT_BRANCHES="$(sh ./governance-pack/scripts/get_config_value.sh governance.exempt_branches "main,develop,staging,dev" | tr -d ' ')"
            if [[ ",${EXEMPT_BRANCHES}," == *",${{ github.ref_name }},"* ]]; then
              echo "Skipping commit validation on exempt branch: ${{ github.ref_name }}"
              exit 0
            fi
          fi
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            RANGE="${{ github.event.pull_request.base.sha }}..${{ github.event.pull_request.head.sha }}"
          else
            DEFAULT_BRANCH="${{ github.event.repository.default_branch }}"
            git fetch --no-tags origin "$DEFAULT_BRANCH":"refs/remotes/origin/$DEFAULT_BRANCH"
            MERGE_BASE="$(git merge-base "refs/remotes/origin/$DEFAULT_BRANCH" "${{ github.sha }}" || true)"
            if [[ -z "$MERGE_BASE" || "$MERGE_BASE" == "${{ github.sha }}" ]]; then
              echo "Using HEAD commit validation (merge-base unavailable or equal to HEAD)."
              RANGE="${{ github.sha }}"
            else
              RANGE="$MERGE_BASE..${{ github.sha }}"
            fi
          fi
          COMMITS=$(git rev-list "$RANGE")
          if [[ -z "$COMMITS" ]]; then
            echo "No commits to validate in range: $RANGE"
            exit 0
          fi
          for commit in $COMMITS; do
            msg="$(git log --format=%s -n 1 "$commit")"
            f="$(mktemp)"
            printf '%s\n' "$msg" > "$f"
            sh ./governance-pack/scripts/validate_commit_msg.sh "$f"
            rm -f "$f"
          done

      - name: Validate PR title
        if: ${{ github.event_name == 'pull_request' }}
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: sh ./governance-pack/scripts/validate_pr_title.sh "$PR_TITLE"

  pr-intelligence:
    name: PR Intelligence Report
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'pull_request' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate PR report
        env:
          PR_BASE_REF: ${{ github.event.pull_request.base.ref }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_BASE_REF: ${{ github.base_ref }}
        run: sh ./governance-pack/scripts/generate_pr_report.sh pr-report.md

      - name: Upload PR report artifact
        uses: actions/upload-artifact@v4
        with:
          name: pr-report
          path: pr-report.md
          if-no-files-found: error

  lint:
    name: Lint
    runs-on: ubuntu-latest
    needs:
      - prepare
      - validate-governance
    if: ${{ needs.prepare.outputs.run_lint == 'true' && needs.prepare.outputs.run_heavy_checks == 'true' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore dependency cache (optional)
        if: ${{ needs.prepare.outputs.cache_enabled == 'true' && needs.prepare.outputs.cache_path != '' && needs.prepare.outputs.cache_key != '' }}
        uses: actions/cache@v4
        with:
          path: ${{ needs.prepare.outputs.cache_path }}
          key: ${{ needs.prepare.outputs.cache_key }}
          restore-keys: ${{ needs.prepare.outputs.cache_restore_keys }}

      - name: Install
        run: ./governance-pack/scripts/run_project_checks.sh install

      - name: Run lint
        run: ./governance-pack/scripts/run_project_checks.sh lint

  test:
    name: Test
    runs-on: ubuntu-latest
    needs:
      - prepare
      - validate-governance
    if: ${{ needs.prepare.outputs.run_test == 'true' && needs.prepare.outputs.run_heavy_checks == 'true' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore dependency cache (optional)
        if: ${{ needs.prepare.outputs.cache_enabled == 'true' && needs.prepare.outputs.cache_path != '' && needs.prepare.outputs.cache_key != '' }}
        uses: actions/cache@v4
        with:
          path: ${{ needs.prepare.outputs.cache_path }}
          key: ${{ needs.prepare.outputs.cache_key }}
          restore-keys: ${{ needs.prepare.outputs.cache_restore_keys }}

      - name: Install
        run: ./governance-pack/scripts/run_project_checks.sh install

      - name: Run tests
        run: ./governance-pack/scripts/run_project_checks.sh test

  build:
    name: Build
    runs-on: ubuntu-latest
    needs:
      - prepare
      - validate-governance
    if: ${{ needs.prepare.outputs.run_build == 'true' && needs.prepare.outputs.run_heavy_checks == 'true' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore dependency cache (optional)
        if: ${{ needs.prepare.outputs.cache_enabled == 'true' && needs.prepare.outputs.cache_path != '' && needs.prepare.outputs.cache_key != '' }}
        uses: actions/cache@v4
        with:
          path: ${{ needs.prepare.outputs.cache_path }}
          key: ${{ needs.prepare.outputs.cache_key }}
          restore-keys: ${{ needs.prepare.outputs.cache_restore_keys }}

      - name: Install
        run: ./governance-pack/scripts/run_project_checks.sh install

      - name: Run build
        run: ./governance-pack/scripts/run_project_checks.sh build

